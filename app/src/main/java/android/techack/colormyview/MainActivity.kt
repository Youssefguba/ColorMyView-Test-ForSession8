package android.techack.colormyview

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class MainActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)


        // TODO (1) Get (id) of the view
        // TODO (2) Make the view Clickable
        // TODO (3) You can make it clickable with the ( When ) Expression
        //  or you can make it by individual view Like this -> view.onClickListener (It's Just an example :D)
    }

}
